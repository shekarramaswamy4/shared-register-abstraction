package client

import (
	"testing"
)

func TestInitialization(t *testing.T) {
	// n1 := node.New()
	// c := New(3, 8080)

	// _, err := c.Read("addr1")
	// assert.NotNil(t, err)

	// err = c.Write("addr1", "val1")
	// assert.Nil(t, err)
	// _, err = c.Read("addr1")
	// assert.NotNil(t, err)
}
